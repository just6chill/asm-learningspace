#################################
# just6chills assembler notes  	#
# made for NASM assembler in  	#
# combination with mingw32      #
# as linker                     #
#################################

Compile:

	Assemble:

		nasm -f win32 filename.asm -o filename.o

	Link:

		gcc test.o -o test.exe

Mnemonics:

  "http://www.mathemainzel.info/files/x86asmref.html"

some Swich case and if else tuts for asm:

  "https://en.wikibooks.org/wiki/X86_Disassembly/Branches"

16 Bytes Memory Registers for 32 bit:

  EAX, EBX, ECX, EDX
  ESP, EBP, ESI, EDI

  Notes:
    ["add esp n" The Stack Pointer will be set back to the given value so that the old Value will be overwritten to save memory]
    [all of these Registers can be used to store variables]


Instruction Sets:
CPU 8086 Assemble only 8086 instruction set
CPU 186 Assemble instructions up to the 80186 instruction set
CPU 286 Assemble instructions up to the 286 instruction set
CPU 386 Assemble instructions up to the 386 instruction set
CPU 486 486 instruction set
CPU 586 Pentium instruction set
CPU PENTIUM Same as 586
CPU 686 P6 instruction set
CPU PPRO Same as 686
CPU P2 Same as 686
CPU P3 Pentium III (Katmai) instruction sets
CPU KATMAI Same as P3
CPU P4 Pentium 4 (Willamette) instruction set
CPU WILLAMETTE Same as P4
CPU PRESCOTT Prescott instruction set
CPU X64 x86-64 (x64/AMD64/Intel 64) instruction set
CPU IA64 IA64 CPU (in x86 mode) instruction set


Base Code:

{"
section .data
  ;define data here

section .text
  ;globals and c includes

  global _WinMain@16
  extern _printf

_WinMain@16
  ;code here
  ret 16
"}

